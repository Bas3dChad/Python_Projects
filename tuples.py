# Tuples are a collection of objects that are orderd and encolsed in prentheses (). Tuples are similar to lists but cannot be modified after creation. Once it is created you cannot add, remove or modify elements within it.

tuple_1 = (1, 2, 3, 'a', 'b', 'c')
print(tuple_1[5])

my_tuple = ("Delware", "Pennsylvania", "New Jersey", "Georgia")
print(my_tuple[2])

